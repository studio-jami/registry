---
type: docs
surface: registry
---

Audit the live registry docs for product-level `suite`, `studio`, and `lane` wording drift.
Remove remaining Harness `lane` prose from customer-facing pages and tighten the generated
bundle README note so the public docs consistently use the locked workspace taxonomy while
retaining existing compatibility route paths.
