---
type: ops
surface: registry
---

Refreshed the hosted public bundle from the current Studio UI generated output. The served
registry now advertises all 20 workspaces (605 items) instead of the stale 4, and
`public/index.html` is the redesigned registry landing. Hand-maintained hosting files
(`_worker.js`, `_headers`, docs, favicon, logo, preview docs) are preserved.
