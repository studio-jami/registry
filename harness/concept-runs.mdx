---
title: "Runs"
sidebarTitle: "Runs"
description: "How Jami Harness models a run — the durable unit of governed agent work, with a stable identifier, actor/project/environment context, and retry and recovery state."
---

<Note>
  **Scaffold page** for the Harness Concepts section. Authored in the Harness documentation
  stream (WS4). The framing below reflects the real Harness responsibility model.
</Note>

A **run** is the durable unit of governed agent work in Jami Harness. Every run has a stable
identifier and carries actor, project, and environment context so its history, retries, and
recovery state are auditable from start to finish.

## On this page (planned)

- The run lifecycle and state machine.
- Run identity and context (actor, project, environment).
- Retry, recovery, and resumability.
- How runs relate to [tasks](/harness/concept-tasks) and [checkpoints](/harness/concept-checkpoints).
