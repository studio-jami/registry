---
title: "Harness Configuration Reference"
sidebarTitle: "Configuration"
description: "Developer reference for configuring Jami Harness — the options, environment, and replaceable ports for storage, providers, memory, and policy."
---

<Note>
  **Scaffold page** for the Harness Reference. The full configuration reference is authored in
  the developer-reference stream (WS6) from the real configuration surface. Secrets are read
  from the environment and are never committed.
</Note>

This page will document how to configure Jami Harness — the options accepted when you create a
harness, the environment it reads, and the replaceable ports for storage, providers,
[memory](/harness/concept-memory), and [policy](/harness/concept-policy).

## Planned reference

- Configuration options and defaults.
- Replaceable adapters (store, provider, memory, policy, trace sinks).
- Environment-driven settings — keys and tokens read from the environment, never hardcoded.
