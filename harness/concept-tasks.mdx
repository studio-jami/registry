---
title: "Tasks"
sidebarTitle: "Tasks"
description: "How Jami Harness models tasks — the units of work inside a run, with their own context, memory, and lifecycle."
---

<Note>
  **Scaffold page** for the Harness Concepts section. Authored in the Harness documentation
  stream (WS4). The framing below reflects the real Harness responsibility model.
</Note>

A **task** is a unit of work inside a [run](/harness/concept-runs). Tasks carry their own
context and memory and move through a defined lifecycle, so the work an agent does is broken
into auditable, recoverable steps rather than one opaque operation.

## On this page (planned)

- Tasks within a run and their lifecycle.
- Task context and [task memory](/harness/concept-memory).
- How tasks invoke [tools](/harness/concept-tools) under [policy](/harness/concept-policy).
