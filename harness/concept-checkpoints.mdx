---
title: "Checkpoints"
sidebarTitle: "Checkpoints"
description: "How Jami Harness uses checkpoints — durable save points that make a run resumable across retries, approvals, and recovery."
---

<Note>
  **Scaffold page** for the Harness Concepts section. Authored in the Harness documentation
  stream (WS4). The framing below reflects the real Harness responsibility model.
</Note>

**Checkpoints** are durable save points in a [run](/harness/concept-runs). They make work
resumable — across retries, around an [approval](/harness/concept-approvals) pause, and
during recovery — so a long or interrupted run continues from where it left off instead of
starting over.

## On this page (planned)

- How checkpoints are created and restored.
- Checkpoints in the runtime lifecycle and state machine.
- Resumability across retries, approvals, and recovery.
