# Studio UI

Studio UI owns the registry item format, authored UI vocabulary, CLI registry lifecycle, renderer, and workbench/showcase surfaces.

Current verified public bundle:

- 45 registry items
- suite bundles for solo, business operations, research writing, and mixed media
- static hosted smoke passing on the Pages preview domain

The public package publish path remains separate from the static registry host and should use trusted npm publishing.
